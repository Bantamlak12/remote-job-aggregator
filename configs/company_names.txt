# One company name per line. Blank lines and lines starting with "#"
# are ignored. search-discover looks up each name via the Serper search
# API and, if it finds a Greenhouse/Lever/Ashby board in the results,
# validates and persists it — no board_url/ats_provider/
# external_board_id needed, unlike configs/seed_companies.json.
#
# All six names below are live-verified: run for real through
# search-discover against the real Serper API and a real Postgres
# database on 2026-09-23, each one found and persisted successfully
# (0 failures). Add more names freely — this file only limits what a
# default, no-argument `search-discover` run searches for; pass a
# different file (`search-discover path/to/names.txt`) to search a
# different list without editing this one.
Spotify
Airbnb
GitLab
Notion
Discord
Figma
